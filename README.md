# API-Data-Explorer-using-Python
This project fetches data from a public REST API, displays it in a user-friendly format, allows searching/filtering, handles API errors, and saves the data into a CSV file.
